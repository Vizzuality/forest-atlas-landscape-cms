<h1>Contexts</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contexts.each do |context| %>
      <tr>
        <td><%= context.name %></td>
        <td><%= link_to 'Show', admin_context_path(context) %></td>
        <td><%= link_to 'Edit', edit_admin_context_path(context) %></td>
        <td><%= link_to 'Destroy', admin_context_path(context), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Context', new_admin_context_path, class: 'btn btn-primary' %>
