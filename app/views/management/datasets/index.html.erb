<%= render partial: 'management/extended_header', locals: {type: 'dataset'} %>
<%= include_gon %>

<%= render partial: 'management/tabs'%>

<%= javascript_pack_tag 'application' %>
<%= react_component('DatasetsPage', {
  datasets: @filtered_datasets
} ) %>