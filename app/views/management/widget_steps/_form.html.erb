<%= javascript_pack_tag 'application' %>

<%= react_component('WidgetsContainer',
                    { datasets: @datasets, widget: @widget }) %>

<%= render partial: 'shared/errors', locals: { resource: @widget } %>
