((function (App) {
  'use strict';
  // XXX : NOT IN USE
  App.Router.FrontHomepage = Backbone.Router.extend({

    routes: {
      '(/)': 'index'
    },

    initialize: function () {
      return null
    },

    index: function () {

    }

  });
})(this.App));
